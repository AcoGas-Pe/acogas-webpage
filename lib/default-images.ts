import { OG_IMAGE_DEFAULTS } from "./seo-config";

/**
 * Rutas de respaldo cuando CMS / catálogo no provee imagen de producto.
 * Alineado con OG_IMAGE_DEFAULTS.productFallback en seo-config.
 */
export const PRODUCT_IMAGE_FALLBACK = OG_IMAGE_DEFAULTS.productFallback;
